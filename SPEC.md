# Spec

No module details live here — each app owns its own spec (`apps/cms-api/SPEC.md`, `apps/cms-admin/SPEC.md`). Active feature spec: `apps/cms-admin/specs/cms-admin-boot-overlay-sequencing.md` (cms-admin boot-overlay sequencing fix, in progress; transient, deleted after its Review step).
