# Spec

No active spec. Start from `CLAUDE.md`; each app owns its spec and docs index
(`apps/<app-name>/SPEC.md`, `apps/<app-name>/docs/ENTRYPOINT.md`). Feature details live in each app's
`docs/documents/*` and must be removed from here once fully captured there (per `docs/workflow.md`'s
Root docs rule).
