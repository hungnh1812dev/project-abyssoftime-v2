node_modules
dist
coverage
# gitignored, generated by postinstall on the host — always regenerated explicitly
# in the `build` stage, so excluding it keeps the build context host-independent
src/prisma/application/client
.git
.gitignore
*.md
.env*
.vscode
test
docs
tasks
plop-templates
Dockerfile
.dockerignore
